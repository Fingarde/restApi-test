function clickLink(link) {
  if (link.style.display == "block") link.style.display = "none";
  else link.style.display = "block";
}

/*
setTimeout(打打打打打打打打打打, 0);
function 打打打打打打打打打打() {
  let elements = document.getElementsByClassName("api-element-identifier");
  Array.prototype.forEach.call(elements, (element) => {

    fs = getComputedStyle(element).fontSize.replace("px", "");
    console.log(fs);
    test = getComputedStyle(element).width;
    console.log(test);
    


    element.style.fontSize = Number.parseInt(fs) + 10 + "px";
  });
}
*/